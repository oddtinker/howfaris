import React from 'react';
import classes from './About.module.css';

const about = () => (
  <div className={classes.About}>
  <h2>About this project</h2>
  <p>Literal social distancing is tough. This app visualizes the distances we're supposed to keep from each other in public spaces.</p>
  </div>
);

export default about;